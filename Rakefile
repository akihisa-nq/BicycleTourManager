# coding: utf-8

GEM_NAME = "bicycle_tour_manager-0.0.1.gem"

file GEM_NAME => ["bicycle_tour_manager.gemspec", "Rakefile"] do
	system("gem build bicycle_tour_manager.gemspec")
end

task :default => GEM_NAME
